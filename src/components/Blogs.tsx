"use server";
import SliderComponent from "./Slider";

const BlogSection = () => {
  return (
    <section id="blogs" className="py-12 px-6">
      <SliderComponent />
    </section>
  );
};

export default BlogSection;
